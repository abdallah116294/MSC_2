import 'package:flutter/material.dart';

class RenewalScreenView extends StatelessWidget {
  const RenewalScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return const RenewalScreenBody();
  }
}
class RenewalScreenBody extends StatelessWidget {
  const RenewalScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Text('Renewal screen'),
    );
  }
}